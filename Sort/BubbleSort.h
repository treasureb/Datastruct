#include<iostream>
#include<assert.h>
using namespace std;

void BubbleSort(int* array,int size)
{
    assert(array);
    for(int end = )
    for(int i = 0;i<size;++i)
    {
        if(array[i] > array[i+1])
            swap(array[i],array[i+1]);
    }
}
